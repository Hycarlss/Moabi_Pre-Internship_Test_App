name: moabi_pre_internship_test_app
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  go_router: ^17.2.3
  google_fonts: ^8.1.0
  provider: ^6.1.2
  intl: ^0.20.2
  shared_preferences: ^2.2.3

  # LOCAL DATABASE
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # AES ENCRYPTION
  encrypt: ^5.0.3

  # ECC
  pointycastle: ^3.9.1

  # STORAGE PATH
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true